# Custom partition table for a 4 MB flash CYD, DESIGN.md SS3/SS5.
#
# Neither PlatformIO/Arduino-ESP32's built-in "default.csv" nor "min_spiffs.csv"
# fits this firmware: default.csv's two 0x140000 (1.25 MiB) OTA app slots are
# smaller than this project's actual firmware.bin (~1.72 MiB as of the
# firmware-skeleton build report - LVGL 9.5 + Wi-Fi/mbedTLS + ESPAsyncWebServer
# + WiFiManager + ArduinoJson add up fast even with every unused LVGL widget
# disabled, see firmware/include/lv_conf.h); min_spiffs.csv's two 0x1E0000
# (1.875 MiB) slots would fit, but its 0x20000 (128 KB) data partition is
# below DESIGN.md SS3's "at least 256 KB" LittleFS minimum. Confirmed both by
# actually building cyd-3248S035R against each and hitting `checkprogsize`
# failures/margins, not by calculation alone.
#
# This table still gives two full OTA app slots and hits the 256 KB LittleFS
# floor exactly, by taking the app slots down to the smallest size the current
# firmware.bin actually fits in with a bit of headroom for growth:
#   nvs      0x9000   0x5000   (20 KB)   - Wi-Fi credentials, standard offset/size
#   otadata  0xe000   0x2000   (8 KB)    - standard OTA selector size
#   app0     0x10000  0x1D0000 (1.813 MiB) - ota_0
#   app1     0x1E0000 0x1D0000 (1.813 MiB) - ota_1
#   spiffs   0x3B0000 0x40000  (256 KB)  - LittleFS (see board_build.filesystem)
#   coredump 0x3F0000 0x10000  (64 KB)   - standard coredump size
# Total: 0x400000 = 4 MiB exactly.
#
# Headroom: ~96 KB per app slot above the current firmware.bin - real but not
# generous. If a future change pushes past it, the first line to reach for is
# trimming firmware/include/lv_conf.h further (drop a Montserrat size, an
# LV_USE_* feature) before growing this table, since growing it means
# shrinking the 256 KB LittleFS floor or dropping to a single (non-OTA) app
# slot.
#
# Name,   Type, SubType, Offset,  Size, Flags
nvs,      data, nvs,     0x9000,  0x5000,
otadata,  data, ota,     0xe000,  0x2000,
app0,     app,  ota_0,   0x10000, 0x1D0000,
app1,     app,  ota_1,   0x1E0000,0x1D0000,
spiffs,   data, spiffs,  0x3B0000,0x40000,
coredump, data, coredump,0x3F0000,0x10000,
